// const admin = require('firebase-admin');

// const db = admin.firestore();

// const queryForUserInExistingGame = async ({ contender, challenger }) => {
//   const currentGamesRef = db.collection('current_games');
//   const contenderInGame = currentGamesRef.where('players', '==', contender.id);
//   console.log(contenderInGame);
// };

// module.exports = {
//   queryForUserInExistingGame,
// };
